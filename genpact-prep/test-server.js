const express = require('express'); const { generateAnswer } = require('./backend/localAI'); const app = express(); app.use(express.json()); app.post('/api/generate', async (req, res) => { try { const answer = await generateAnswer('Question: What is Belady\\'s Anomaly?', 'Technical', 'Humble', 'Software Engineer', 'CS Fundamentals'); res.json({ content: answer }); } catch (e) { res.status(500).json({ error: e.message }); } }); app.listen(5005, () => console.log('Ready'));
